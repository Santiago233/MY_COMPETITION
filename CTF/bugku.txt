MISC
1.签到题：关注公众号获取

2.这是一张单纯的图片：利用010打开，用最后面的Unicode解码

3.隐写：利用010打开，修改（增加）图片的长度（因为此时只有一个报错，说明图片被修改了）

4.telnet：利用wireshark打开后，查找telnet数据报，追踪流即可获得flag

5.眼见非实：根据img经验判断zip然后改后缀解压缩，得到的docx其实还是zip再改后缀解压缩，最后在xml里面寻找flag

6.又一张图片还单纯吗：利用binwalk –e对图片进行提取，然后对相应的图片部分进行提取部分0，1文件读取得到flag（复制，另存为text，然后以hex格式读取）

7.猜： 竟然是把图片丢到百度识图

8.宽带信息泄露：Bin格式为路由流量包格式，利用routerpassview分析得到用户名即可

9.隐写2：binwalk扫描，解压缩，010打开得到最后一段base64解码

10.多种方式解决：利用010打开KEY.EXE文件然后读取其中的base64部分，利用指令base64 –d 读取后的文件名 > 生成文件名

11.Linux：下载文件解压缩——一定要在linux下解压缩（tar –zxvf 文件名），windows下解压缩生成的文件不对，之后使用cat打开

12.中国菜刀：分析得到flag文件，之后binwalk提取（或者根据挂马来判断，字节导出后删除前后的x@y，由压缩转为acsii即可得到）

13.这么多数据包：即拿到机器的权限——找到其中getshell的数据包，然后进行base64密码的读取和解码，得到flag

14.做个游戏：很神奇的一个题目，需要先把jar文件解压缩——对，很神奇，然后对其中文件进行反编译，得到flag（.class为java类语言，需要一个jar反编译程序执行）

15.想蹭网先解开密码：WPA数据包密码，四位需要直接爆破，首先根据既有的七位生成词典(-w)。之后利用kali里面的aircrack–ng跑一下，得到flag(再复杂一点，即原本就是被加密的直接套用airdecap –ng利用跑出的密码访问网址，需要-e，-p)

16.Linux2：首先，得知道一般文件处理办法，binwalk拆分，还有foremost和extundelete；此外，学会使用grep直接查找文件中的关键词也很重要

17.账号被盗了：mmp网站挂了

18.细心的大象：图片隐写一个rar文件——需要通过010editor分离文件结构得到文件尾，发现后面有一个rar文件，后续文件粘贴到新的file中得到rar，解压密码在图片的注释里面（base64加密）,最后再解压缩修改图片长度得到图片的原型

19.爆照：binwalk拆分穹妹照片，得到的压缩文件夹windows解压缩后再放入kali，得到一堆图片和一张gif，所以真实格式是.jpg格式。直接扫码，将文件恢复jpg格式后查看备注，binwalk生成分别得到每段密码，组合成flag

20.猫片：照片放到stegslove里面跑一下貌似有一张二维码，所以直接选择生成，然后截取里面的图片得到半张二维码，然后对图片进行010editor处理得到全张图片。很坑的在于我没看出来这张二维码需要反色，狂扫了好久没有结果。最后扫到一个压缩文件，但是压缩文件表示坏掉了，wtf。参考网上做法ntfs抓取数据流量，对得到的pyc进行反编译，再根据加密写解密脚本，好难

21.啊哒：这题是真的狗……让我以为是伪加密，还给我CRC数据……结果又是把密码放在文件信息里面，将16进制转换为字符串解压缩就可以得到结果

22.闪的好快：每一个被剪开的二维码可以扫出对应的字符，拼凑在一起就是flag的内容

23.Come_game：游戏不会，存档里面010打开观察是总共几关读取的时候就会自动显示通过，所以直接修改就可以了（后面记得全部归0就好了）

24.白哥的鸽子：010最后一部分栅栏密码处理就好了

25.多彩：这题很坑，stegsolve需要多试试出现一个zip包，神奇的是不知道为啥win10和kali下打不开，需要到ubuntu下才能看到里面有个flag.txt包，但是这个不是伪加密，密码就是颜色变化，图片名字是“口红”，用颜色代表口红标号，然后转换成二进制再转换成字符串，得到密码。关键问题来了…密码输入不正确，没事搞什么中文…就丢掉不管了

26.旋转跳跃：先尝试binwalk和strings，之后直接使用MP3Stego加解密即可

27.普通的二维码：二维码图片使用010打开之后把最后一段取出来，八进制写脚本(3个一组)转换为ASCII码

28.乌云邀请码：直接StegSolve扫一下，选择是空，0，0，0；Row；LSB First；BGR

29.神秘的文件：首先是感谢这道题我学会了明文爆破，之前做不了不是软件没用而是自己不会用。解压开来的doc其实吧…里面也影藏了一个flag，只不过这个没用，还得重新binwalk这个doc找到flag.txt再解密

30.论剑：知识点还比较多，首先010查看里面的Hex码发现有一串极其规律的0-1序列串，整理出来发现是可以ASCII转译（仔细看Hex码内容）；修改图片的高度，方法是010种下载JPG.bt，修改WORD X/Y image（即宽和高），查看影藏内容，所以还是需要不同的bt文件；再仔细查看0-1序列串周围的内容，发现一个貌似7z文件的文件头，修改头文件使其成为7z文件的文件头——37 7A BC AF 1C，其实就是一般文件隐写如果有内容基本还是隐写了压缩文件夹。解析出来的图片一样的处理方法发现和之前的刚好互补，之后base16解析——说一下几个的区别：16（0-9，A-F，不可能有=）；32（2-7，A-Z）；64（0-9，A-Z，a-z，/，+）。这一题我还是很涨知识的

31.图穷匕见：根据JPEG文件格式，取出最后面的内容，这是十六进制文件，利用nodepad++进行插件转换，得到hex内容为坐标然后在gnuplot进行绘制（该死的软件，太难用了…指令plot ‘文件名’）

32.Convert：二进制转十六进制，然后生成解压缩RAR文件，再查看详细信息里面的base64解密

二、WEB
1.web2:直接查看源代码看注释

2.计算器：直接wxml里面修改maxlength

3.web基础&get：根据php语法数组，直接后面添加?what=flag

4.web基础&post:相比4，只要修改一下传送的方式就可以了，将原来的get方式换成post方式

5.矛盾：通过注入使得产生or 1=1的格式得到flag

6.web3：查看网页源代码，最后一行注释的部分unicode转换为acsii

7.域名解析：在c:\windows\system32\drivers\etc\hosts目录下添加120.24.86.145 flag.bugku.com，然后直接爆出key

8.你必须让他停下来：疯狂的刷新直到出现flag

9.本地包含：因为替换的是hello元素，所以将hello元素内容可以赋值为需要打印的元素，例如print_r(file(“flag.php”))

10.变量1：这就一道坑爹的脑洞题，flag.php里面定义好了flag在全局变量里面，所以直接GLOBALS

11.Web5:直接性感jsfuck，在线f12编码回车打印

12.头等舱：利用burpsuit在线抓包，抓包后重放查看……怎么想的都不知道

13.网站被黑：主要就是利用神器御风，找到影藏在背后的网页，之后再burpsuite强行爆破就好了（导入字典，最后选择长度不一样的作为尝试），提示社区版本无法爆破

14.web4：其实我没看懂源码，直接把一个比较的数据放进去比较了一下就得到key了，注意：控制台里面可以自动转化escape加密

15.flag在index里面：需要使用php伪协议，这里面因为是file需要选取对应的file伪协议

16.输入密码查看flag:5位数字密码爆破，为什么爆破的时候总告诉我payload设置错误

17.点击一百万次：firefox有插件HackBar，了解一下。里面直接写入clicks=1000000以及网址然后execute

18.管理员系统：js查看得到注释，base64解码这就是密码，然后尝试用户名admin，密码test123（解密结果），但是发现ip被封了，没想到吧（突然想到之前分享说的是找一堆代理然后列表尝试——这是爆破的方法）然后burpsuit抓包重放的里面修改头，添加X-Forwarded-For，后面接的代理的ip

19.备份是个好习惯：这是一个类似于源码泄露的题目，和在XMAN做的题目很像，备份文件.bak格式，可以御剑扫一下有什么隐藏的网页，在后面添加index.php.bak，得到泄露的源码。之后便是认真分析源码（不详细解释这一题，一定要好好分析），对于md5()函数的绕过有两种办法：md5()函数是不检查数组格式的，即强行添加[]；另一种是md5()加密后为0exxxxx形式，则被认为是科学记数法，即为0

20.成绩单：sql手工注入三连（甚至可以说是四连：默写一遍）：
先检查需要填入的变量名称，得到为id：
id=1’ union select 1,2,3,database()# ->得到数据库名称
id=1’ union select 1,2,3,table_name from information_schema.tables where table_schema=database()#->得到表名fl4g（神奇得不行，需要复制后重新输入一遍’）
id=1’ union select 1,2,3,column_name from information_schema.columns where table_name=’fl4g’#->得到列名skctf_flag
id=1’ union select 1,2,3,skctf_flag from fl4g#->得到flag；
group_concat(例如’table name’，主要是让所有的显示在一行，最好都加上，因为自己被坑了)

21.秋名山老司机：编写脚本，利用requests.get正则表达式获取网页里面的关系式内容并进行处理，得到的结果在requests.post传到网页获取flag（PS：因为网页是刷新改变的，所以需要使用requests.session()）

22.我觉得你得快点：编写脚本，requests.session()同上一题。主要需要传送给网页的内容来自get到的headers里面的flag，其他的编写脚本部分同上

23.cookies欺骗：base64解码发现可以修改filename文件名得到源代码，一般需要多试试index.php，得到源代码解析，发现需要在header响应头中添加margin头，然后就很简单了

24.never give up：都是些什么神仙题目……查看源代码里面的注释其实是个网页(所有的网页都是在test目录下的)…然后网页还被重定向了，所以只能利用view-source(网页地址)，肉眼可见的部分base64解码，url解码，得到源代码…现在问题来了，都不要看逻辑的嘛？？？直接就可以访问那个目录下的文件？我透……（而且我不是很明白为什么不能复现别人wp的结果）
其一：file_get_contents需要使用PHP伪协议，输入流(php://input)——不能出现某一字符但是后面必须使用
其二：需要使用eregi()的空字符检测漏洞，以\x00（url编码：%00）
其三：PHP字符与int型数值弱比较，若字符以数字开头则数字作为若转换的结果，不然就是0